diff --git a/static/app.css b/static/app.css
index d261b72..0905779 100644
--- a/static/app.css
+++ b/static/app.css
@@ -1,4 +1,4 @@
-:root{--bg:#f3f6fb;--surface:#fff;--line:#e5eaf2;--text:#172033;--muted:#738097;--blue:#3867f5;--violet:#7c4dff;--green:#14a06f;--amber:#d58b16;--red:#d84c4c;--sidebar:#121a2c}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:Inter,"Segoe UI","PingFang SC","Microsoft YaHei",sans-serif;padding-left:244px}.sidebar{position:fixed;inset:0 auto 0 0;width:244px;background:linear-gradient(180deg,#111a2d,#17223a);color:#fff;padding:24px 16px;display:flex;flex-direction:column;gap:8px}.brand{display:flex;align-items:center;gap:12px;padding:4px 8px 26px}.brand-mark{display:grid;place-items:center;width:40px;height:40px;border-radius:12px;background:linear-gradient(135deg,#5a7cff,#8d58f6);font-size:20px;font-weight:900;box-shadow:0 9px 22px #4565ed55}.brand strong{display:block;font-size:15px}.brand small{display:block;color:#9daaca;margin-top:4px}.nav{border:0;background:transparent;color:#b8c3dd;border-radius:10px;padding:12px 14px;text-align:left;font:inherit;font-weight:650;cursor:pointer}.nav:hover,.nav.active{background:#ffffff16;color:#fff}.nav.active{box-shadow:inset 3px 0 #7190ff}.side-note{margin-top:auto;padding:14px;color:#8997b7;font-size:12px;line-height:1.65;border-top:1px solid #ffffff12}main{padding:34px;max-width:1600px;margin:auto}header{display:flex;justify-content:space-between;gap:20px;align-items:flex-start;margin-bottom:24px}.eyebrow{font-weight:800;font-size:11px;letter-spacing:.18em;color:var(--blue);margin:0 0 8px}h1{margin:0;font-size:28px}header .sub{margin:9px 0 0;color:var(--muted)}.health{display:flex;align-items:center;gap:8px;background:#e9f9f2;color:#137253;padding:9px 12px;border-radius:999px;font-size:12px;font-weight:700}.health span{width:8px;height:8px;border-radius:50%;background:#1aba7a;box-shadow:0 0 0 5px #1aba7a22}.stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:14px;margin-bottom:14px}.stat{background:var(--surface);border:1px solid var(--line);border-radius:14px;padding:16px;box-shadow:0 8px 24px #24406b0a}.stat span{display:block;color:var(--muted);font-size:12px}.stat strong{display:block;font-size:25px;margin-top:7px}.eligibility-note{display:flex;gap:12px;align-items:center;padding:11px 14px;margin:0 0 18px;border:1px solid #f1dfbb;border-radius:11px;background:#fffaf0;color:#78613a;font-size:12px;line-height:1.5}.eligibility-note strong{color:#a26912;white-space:nowrap}.logic-note{display:flex;gap:12px;align-items:flex-start;padding:12px 14px;margin:0 0 18px;border:1px solid #cfe0ff;border-radius:11px;background:#f4f8ff;color:#4d6388;font-size:12px;line-height:1.6}.logic-note strong{color:#315db7;white-space:nowrap}.reason-cell{max-width:360px;overflow:hidden;text-overflow:ellipsis}.grid.two,.grid.three{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.grid.three{grid-template-columns:repeat(3,minmax(0,1fr))}.card{background:var(--surface);border:1px solid var(--line);border-radius:16px;padding:20px;box-shadow:0 10px 28px #213b670b;margin-bottom:18px}.card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:18px}.card-head>div:first-child{min-width:0}.card-head h2{display:inline;margin:0 0 0 8px;font-size:17px}.card-head p,.card>p{color:var(--muted);font-size:13px;line-height:1.6}.step{display:inline-grid;place-items:center;width:30px;height:30px;border-radius:9px;background:#edf2ff;color:var(--blue);font-weight:850}.step.violet{background:#f2edff;color:var(--violet)}.step.green{background:#e9f8f2;color:var(--green)}.step.amber{background:#fff5df;color:var(--amber)}.format{padding:6px 9px;border:1px solid var(--line);border-radius:8px;color:var(--muted);font-size:11px;white-space:nowrap}textarea{width:100%;height:112px;resize:vertical;border:1px solid #dbe2ec;border-radius:10px;padding:12px;font:12px/1.55 ui-monospace,Consolas,monospace;color:#344158;outline:0;background:#fbfcfe;margin:3px 0 12px}textarea:focus{border-color:#8ca6ff;box-shadow:0 0 0 3px #4c70ef16}.btn{display:inline-flex;min-height:36px;padding:0 14px;align-items:center;justify-content:center;border:1px solid transparent;border-radius:9px;font:inherit;font-size:13px;font-weight:750;cursor:pointer;text-decoration:none}.btn.primary{background:var(--blue);color:#fff}.btn.violet{background:var(--violet);color:#fff}.btn.success{background:var(--green);color:#fff}.btn.ghost{background:#fff;border-color:#dce3ed;color:#46546b}.btn:disabled{opacity:.5;cursor:not-allowed}.pairing-card,.result-card{padding:0}.pairing-card .card-head,.result-card .card-head{padding:20px}.card-head.responsive{align-items:center}.actions{display:flex;align-items:center;gap:9px;flex-wrap:wrap}.actions label{color:var(--muted);font-size:12px}.actions input[type=number]{width:62px;margin-left:5px;border:1px solid #dce3ed;border-radius:8px;height:36px;padding:0 8px}.table-wrap{overflow:auto;border-top:1px solid var(--line)}table{border-collapse:collapse;width:100%;font-size:13px}th,td{padding:12px 15px;border-bottom:1px solid #edf0f5;text-align:left;white-space:nowrap}th{color:#748197;font-size:11px;text-transform:uppercase;letter-spacing:.04em;background:#fafbfc}tbody tr:hover td{background:#fbfcff}.pill{display:inline-flex;padding:4px 8px;border-radius:999px;font-size:11px;font-weight:750;background:#edf1f7;color:#5d6a80}.pill.success,.pill.available,.pill.ready{background:#e8f8f1;color:#11805a}.pill.running,.pill.queued,.pill.reserved{background:#edf2ff;color:#3f63d8}.pill.failed{background:#ffeded;color:#bd4141}.pill.review{background:#fff3d7;color:#9b6710}.task-list{border-top:1px solid var(--line)}.result-task-list{max-height:560px;overflow-y:auto}.task-row{display:grid;grid-template-columns:34px 70px minmax(240px,1fr) 130px minmax(200px,1.2fr) 110px;gap:12px;padding:13px 20px;border-bottom:1px solid #edf0f5;align-items:center;font-size:13px}.task-row small{color:var(--muted)}.hidden{display:none!important}#toast{position:fixed;right:24px;bottom:24px;max-width:480px;background:#162033;color:#fff;padding:12px 16px;border-radius:10px;box-shadow:0 16px 40px #11182744;opacity:0;transform:translateY(8px);transition:.2s;pointer-events:none;font-size:13px}#toast.show{opacity:1;transform:none}@media(max-width:1050px){body{padding-left:0}.sidebar{position:static;width:auto;height:auto;flex-direction:row;align-items:center;padding:12px;overflow:auto}.brand{padding:0;margin-right:auto}.brand small,.side-note{display:none}.nav{white-space:nowrap}main{padding:20px}.grid.two,.grid.three{grid-template-columns:1fr}.stats{grid-template-columns:repeat(2,1fr)}}@media(max-width:650px){main{padding:14px}.stats{grid-template-columns:1fr 1fr}.card-head.responsive,header{display:block}.actions{margin-top:14px}.task-row{grid-template-columns:1fr}.health{margin-top:14px;width:max-content}}
+:root{--bg:#f3f6fb;--surface:#fff;--line:#e5eaf2;--text:#172033;--muted:#738097;--blue:#3867f5;--violet:#7c4dff;--green:#14a06f;--amber:#d58b16;--red:#d84c4c;--sidebar:#121a2c}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:Inter,"Segoe UI","PingFang SC","Microsoft YaHei",sans-serif;padding-left:244px}.sidebar{position:fixed;inset:0 auto 0 0;width:244px;background:linear-gradient(180deg,#111a2d,#17223a);color:#fff;padding:24px 16px;display:flex;flex-direction:column;gap:8px}.brand{display:flex;align-items:center;gap:12px;padding:4px 8px 26px}.brand-mark{display:grid;place-items:center;width:40px;height:40px;border-radius:12px;background:linear-gradient(135deg,#5a7cff,#8d58f6);font-size:20px;font-weight:900;box-shadow:0 9px 22px #4565ed55}.brand strong{display:block;font-size:15px}.brand small{display:block;color:#9daaca;margin-top:4px}.nav{border:0;background:transparent;color:#b8c3dd;border-radius:10px;padding:12px 14px;text-align:left;font:inherit;font-weight:650;cursor:pointer}.nav:hover,.nav.active{background:#ffffff16;color:#fff}.nav.active{box-shadow:inset 3px 0 #7190ff}.side-note{margin-top:auto;padding:14px;color:#8997b7;font-size:12px;line-height:1.65;border-top:1px solid #ffffff12}main{padding:34px;max-width:1600px;margin:auto}header{display:flex;justify-content:space-between;gap:20px;align-items:flex-start;margin-bottom:24px}.eyebrow{font-weight:800;font-size:11px;letter-spacing:.18em;color:var(--blue);margin:0 0 8px}h1{margin:0;font-size:28px}header .sub{margin:9px 0 0;color:var(--muted)}.health{display:flex;align-items:center;gap:8px;background:#e9f9f2;color:#137253;padding:9px 12px;border-radius:999px;font-size:12px;font-weight:700}.health span{width:8px;height:8px;border-radius:50%;background:#1aba7a;box-shadow:0 0 0 5px #1aba7a22}.stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:14px;margin-bottom:14px}.stat{background:var(--surface);border:1px solid var(--line);border-radius:14px;padding:16px;box-shadow:0 8px 24px #24406b0a}.stat span{display:block;color:var(--muted);font-size:12px}.stat strong{display:block;font-size:25px;margin-top:7px}.eligibility-note{display:flex;gap:12px;align-items:center;padding:11px 14px;margin:0 0 18px;border:1px solid #f1dfbb;border-radius:11px;background:#fffaf0;color:#78613a;font-size:12px;line-height:1.5}.eligibility-note strong{color:#a26912;white-space:nowrap}.logic-note{display:flex;gap:12px;align-items:flex-start;padding:12px 14px;margin:0 0 18px;border:1px solid #cfe0ff;border-radius:11px;background:#f4f8ff;color:#4d6388;font-size:12px;line-height:1.6}.logic-note strong{color:#315db7;white-space:nowrap}.reason-cell{max-width:360px;overflow:hidden;text-overflow:ellipsis}.grid.two,.grid.three{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.grid.three{grid-template-columns:repeat(3,minmax(0,1fr))}.card{background:var(--surface);border:1px solid var(--line);border-radius:16px;padding:20px;box-shadow:0 10px 28px #213b670b;margin-bottom:18px}.card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:18px}.card-head>div:first-child{min-width:0}.card-head h2{display:inline;margin:0 0 0 8px;font-size:17px}.card-head p,.card>p{color:var(--muted);font-size:13px;line-height:1.6}.step{display:inline-grid;place-items:center;width:30px;height:30px;border-radius:9px;background:#edf2ff;color:var(--blue);font-weight:850}.step.violet{background:#f2edff;color:var(--violet)}.step.green{background:#e9f8f2;color:var(--green)}.step.amber{background:#fff5df;color:var(--amber)}.format{padding:6px 9px;border:1px solid var(--line);border-radius:8px;color:var(--muted);font-size:11px;white-space:nowrap}textarea{width:100%;height:112px;resize:vertical;border:1px solid #dbe2ec;border-radius:10px;padding:12px;font:12px/1.55 ui-monospace,Consolas,monospace;color:#344158;outline:0;background:#fbfcfe;margin:3px 0 12px}textarea:focus{border-color:#8ca6ff;box-shadow:0 0 0 3px #4c70ef16}.btn{display:inline-flex;min-height:36px;padding:0 14px;align-items:center;justify-content:center;border:1px solid transparent;border-radius:9px;font:inherit;font-size:13px;font-weight:750;cursor:pointer;text-decoration:none}.btn.primary{background:var(--blue);color:#fff}.btn.violet{background:var(--violet);color:#fff}.btn.success{background:var(--green);color:#fff}.btn.ghost{background:#fff;border-color:#dce3ed;color:#46546b}.btn:disabled{opacity:.5;cursor:not-allowed}.pairing-card,.result-card{padding:0}.pairing-card .card-head,.result-card .card-head{padding:20px}.card-head.responsive{align-items:center}.actions{display:flex;align-items:center;gap:9px;flex-wrap:wrap}.actions label{color:var(--muted);font-size:12px}.actions input[type=number],.actions select{margin-left:5px;border:1px solid #dce3ed;border-radius:8px;height:36px;padding:0 8px;background:#fff;color:var(--text)}.actions input[type=number]{width:62px}.table-wrap{overflow:auto;border-top:1px solid var(--line)}table{border-collapse:collapse;width:100%;font-size:13px}th,td{padding:12px 15px;border-bottom:1px solid #edf0f5;text-align:left;white-space:nowrap}th{color:#748197;font-size:11px;text-transform:uppercase;letter-spacing:.04em;background:#fafbfc}tbody tr:hover td{background:#fbfcff}.pill{display:inline-flex;padding:4px 8px;border-radius:999px;font-size:11px;font-weight:750;background:#edf1f7;color:#5d6a80}.pill.success,.pill.available,.pill.ready{background:#e8f8f1;color:#11805a}.pill.running,.pill.queued,.pill.reserved{background:#edf2ff;color:#3f63d8}.pill.failed{background:#ffeded;color:#bd4141}.pill.review{background:#fff3d7;color:#9b6710}.task-list{border-top:1px solid var(--line)}.result-task-list{max-height:560px;overflow-y:auto}.task-row{display:grid;grid-template-columns:34px 70px minmax(240px,1fr) 130px minmax(200px,1.2fr) 110px;gap:12px;padding:13px 20px;border-bottom:1px solid #edf0f5;align-items:center;font-size:13px}.task-row small{color:var(--muted)}.hidden{display:none!important}#toast{position:fixed;right:24px;bottom:24px;max-width:480px;background:#162033;color:#fff;padding:12px 16px;border-radius:10px;box-shadow:0 16px 40px #11182744;opacity:0;transform:translateY(8px);transition:.2s;pointer-events:none;font-size:13px}#toast.show{opacity:1;transform:none}@media(max-width:1050px){body{padding-left:0}.sidebar{position:static;width:auto;height:auto;flex-direction:row;align-items:center;padding:12px;overflow:auto}.brand{padding:0;margin-right:auto}.brand small,.side-note{display:none}.nav{white-space:nowrap}main{padding:20px}.grid.two,.grid.three{grid-template-columns:1fr}.stats{grid-template-columns:repeat(2,1fr)}}@media(max-width:650px){main{padding:14px}.stats{grid-template-columns:1fr 1fr}.card-head.responsive,header{display:block}.actions{margin-top:14px}.task-row{grid-template-columns:1fr}.health{margin-top:14px;width:max-content}}
 
 .keep-open-note{border-color:#bfe9d7;background:#f1fbf7;color:#3f6f5e}.keep-open-note strong{color:#16805d}.pill.closed{background:#edf1f7;color:#637086}.row-actions{display:flex;gap:8px;align-items:center}.btn.danger{background:#fff;border-color:#f1caca;color:var(--red)}.btn.danger:hover{background:#fff3f3}.action-error{display:block;max-width:340px;margin-top:5px;color:#bd4141;overflow:hidden;text-overflow:ellipsis}
 
diff --git a/static/app.js b/static/app.js
index 1137015..2661bf2 100644
--- a/static/app.js
+++ b/static/app.js
@@ -4,6 +4,7 @@ let STATE = {accounts:[], replacements:[], proxies:[], tasks:[], summary:{}};
 let selected = new Set();
 let selectionInitialized = false;
 let selectedSuccessAccounts = new Set();
+let successTrialFilter = 'all';
 let selectedResultTasks = new Set();
 let selectedReplacements = new Set();
 let pairs = [];
@@ -20,12 +21,13 @@ function renderPairs(){const eligible=STATE.accounts.filter(a=>['ready','failed'
 function renderPool(){const rows=STATE.replacements;const valid=new Set(rows.map(r=>Number(r.id)));selectedReplacements=new Set([...selectedReplacements].filter(id=>valid.has(Number(id))));$('#poolBody').innerHTML=rows.map(r=>{const busy=r.status==='reserved'||STATE.tasks.some(t=>Number(t.replacement_id)===Number(r.id)&&['queued','running'].includes(t.status));return `<tr><td><input type="checkbox" data-replacement-id="${r.id}" ${selectedReplacements.has(Number(r.id))?'checked':''}></td><td>#${r.id}</td><td><strong>${esc(r.email)}</strong></td><td>${r.has_api?'已配置':'缺失'}</td><td>${pill(r.status)}</td><td>${esc(r.bound_old_email||'-')}</td><td class="reason-cell" title="${esc(r.failure_reason||'')}">${esc((r.failure_reason||'-').slice(0,80))}</td><td><div class="row-actions">${r.status==='failed'?`<button class="btn ghost" data-restore-replacement="${r.id}">重新启用</button>`:''}<button class="btn danger" data-delete-replacement="${r.id}" data-label="${esc(r.email)}" ${busy?'disabled title="活动任务正在占用"':''}>删除</button></div></td></tr>`}).join('')||'<tr><td colspan="8">号池为空</td></tr>';$('#poolBody').querySelectorAll('[data-replacement-id]').forEach(el=>el.onchange=()=>{const id=Number(el.dataset.replacementId);el.checked?selectedReplacements.add(id):selectedReplacements.delete(id);renderPool()});const all=rows.length>0&&rows.every(r=>selectedReplacements.has(Number(r.id)));['#selectReplacementAll','#selectReplacementAllTable'].forEach(selector=>{const input=$(selector);if(input)input.checked=all});$('#deleteSelectedReplacements').disabled=!selectedReplacements.size}
 function renderProxyPool(){$('#proxyPoolBody').innerHTML=STATE.proxies.map(r=>{const busy=STATE.tasks.some(t=>Number(t.proxy_id)===Number(r.id)&&['queued','running'].includes(t.status));return `<tr><td>#${r.id}</td><td><strong>${esc(r.display)}</strong></td><td>${pill(r.status)}</td><td>${esc(r.success_count||0)} / ${esc(r.failure_count||0)}</td><td>${esc(r.last_exit_ip||'-')}${r.last_country?` · ${esc(r.last_country)}`:''}</td><td>${esc(r.last_old_email||'-')}</td><td class="reason-cell" title="${esc(r.failure_reason||'')}">${esc((r.failure_reason||'-').slice(0,80))}</td><td><div class="row-actions">${r.status==='failed'?`<button class="btn ghost" data-restore-proxy="${r.id}">重新启用</button>`:''}<button class="btn danger" data-delete-proxy="${r.id}" data-label="${esc(r.display)}" ${busy?'disabled title="活动任务正在使用"':''}>删除</button></div></td></tr>`}).join('')||'<tr><td colspan="8">代理池为空，请先手动导入。</td></tr>'}
 function renderDetectionProxies(){const rows=STATE.detection_proxies||[];const summary=$('#detectionProxySummary');const body=$('#detectionProxyBody');const deleteAll=$('#deleteAllDetectionProxies');if(!summary)return;if(!rows.length){summary.textContent='尚未添加检测代理';if(body)body.innerHTML='<tr><td colspan=6>暂无检测代理，请先导入。</td></tr>';if(deleteAll)deleteAll.disabled=true;return}const groups={};rows.forEach(row=>{const country=String(row.country||row.last_country||'未标注').toUpperCase();groups[country]=(groups[country]||0)+1});summary.textContent=`已添加 ${rows.length} 条检测代理 · 国家：${Object.entries(groups).map(([country,count])=>`${country}（${count}）`).join('、')}`;if(deleteAll)deleteAll.disabled=false;if(!body)return;body.innerHTML=rows.map(row=>{const busy=STATE.accounts.some(account=>['queued','running'].includes(account.trial_check_status)&&Number(account.trial_check_proxy_id)===Number(row.id));const status=row.status==='failed'?'failed':'available';const lastCheck=row.last_check_at||'-';return `<tr><td>#${row.id}</td><td><strong>${esc(row.display||'-')}</strong></td><td>${esc(row.country||row.last_country||'未标注')}</td><td>${pill(status)}</td><td>${esc(row.last_exit_ip||'-')}</td><td>${esc(lastCheck)}${busy?' · 使用中':''}</td></tr>`}).join('')}
-function renderSuccessAccounts(){const rows=STATE.accounts.filter(a=>a.status==='success');const valid=new Set(rows.map(a=>Number(a.id)));selectedSuccessAccounts=new Set([...selectedSuccessAccounts].filter(id=>valid.has(Number(id))));$('#successAccountsBody').innerHTML=rows.map(a=>{const refreshing=a.at_refresh_status==='running';const closed=a.roxy_browser_status==='closed';const deleted=a.roxy_browser_status==='deleted';const notOpened=a.roxy_browser_status==='not_opened';const openFailed=a.roxy_browser_status==='open_failed';const hasProfile=Boolean(a.roxy_profile_id);const atLabel=refreshing?'获取中':a.at_refresh_status==='failed'?'失败':!a.at_saved?'未保存':a.at_token_changed===true?'已更新':'已保存';const windowState=deleted?'deleted':closed?'closed':openFailed?'open_failed':notOpened?'not_opened':'success';const windowLabel=deleted?'已删除':closed?'已关闭':openFailed?'打开失败':notOpened?'纯协议':'已打开';const closeLabel=deleted?'已删':closed?'删环境':'关闭';return `<tr><td><input type="checkbox" data-success-account-id="${a.id}" ${selectedSuccessAccounts.has(Number(a.id))?'checked':''}></td><td><div class="email-stack"><div><span>原</span><strong>${esc(a.old_email||'-')}</strong></div><div><span>新</span><strong>${esc(a.new_email||a.current_email||'-')}</strong></div></div></td><td>${pill(refreshing?'running':a.at_refresh_status==='failed'?'failed':'success')}<small class="compact-label">${esc(atLabel)}</small>${a.at_refresh_error?`<small class="action-error compact-error" title="${esc(a.at_refresh_error)}">⚠</small>`:''}</td><td title="${esc(a.roxy_open_error||'')}">${pill(windowState)}<small class="compact-label">${esc(windowLabel)}</small>${openFailed?'<small class="action-error compact-error">⚠</small>':''}</td><td><span class="cdp-port">${a.roxy_cdp_port?`<code>${esc(a.roxy_cdp_port)}</code>`:'-'}</span></td><td>${esc(shortTime(a.at_saved_at||a.at_refreshed_at||a.rebound_at))}</td><td><div class="row-actions"><button class="btn ghost" title="复制完整结果" data-copy-export="${a.id}">结果</button><button class="btn primary" title="复制 AT" data-copy-at="${a.id}">AT</button><button class="btn violet" title="推送 AT 到 GCash 提链" data-push-gcash="${a.id}">GCash</button><button class="btn primary" title="检查并更新 AT（Roxy 窗口或纯协议登录）" data-refresh-at="${a.id}" ${refreshing?'disabled':''}>${refreshing?'获取中':'刷新'}</button><button class="btn ghost" title="关闭窗口或删除环境" data-close-roxy="${a.id}" ${refreshing||!hasProfile||deleted?'disabled':''}>${closeLabel}</button><button class="btn danger" title="清理账号" data-delete-success-account="${a.id}" data-label="${esc(a.new_email||a.current_email||a.old_email)}" ${refreshing||hasProfile?'disabled title="请先关闭窗口并删除 Roxy 环境"':''}>清理</button></div></td></tr>`}).join('')||'<tr><td colspan="7">暂无换绑完成账号。</td></tr>';$('#successAccountsBody').querySelectorAll('[data-success-account-id]').forEach(el=>el.onchange=()=>{const id=Number(el.dataset.successAccountId);el.checked?selectedSuccessAccounts.add(id):selectedSuccessAccounts.delete(id);renderSuccessAccounts()});const all=rows.length>0&&rows.every(a=>selectedSuccessAccounts.has(Number(a.id)));['#selectSuccessAll','#selectSuccessAllTable'].forEach(selector=>{const input=$(selector);if(input)input.checked=all});$('#copySelectedAT').disabled=!selectedSuccessAccounts.size;$('#pushSelectedGCash').disabled=!selectedSuccessAccounts.size;$('#cleanupSelectedAccounts').disabled=!selectedSuccessAccounts.size}
+function successAccountRows(){const rows=STATE.accounts.filter(a=>a.status==='success');return successTrialFilter==='trial'?rows.filter(a=>a.trial_check_status==='success'&&a.trial_zero_trial_eligible===true):rows}
+function renderSuccessAccounts(){const rows=successAccountRows();const valid=new Set(rows.map(a=>Number(a.id)));selectedSuccessAccounts=new Set([...selectedSuccessAccounts].filter(id=>valid.has(Number(id))));$('#successAccountsBody').innerHTML=rows.map(a=>{const refreshing=a.at_refresh_status==='running';const closed=a.roxy_browser_status==='closed';const deleted=a.roxy_browser_status==='deleted';const notOpened=a.roxy_browser_status==='not_opened';const openFailed=a.roxy_browser_status==='open_failed';const hasProfile=Boolean(a.roxy_profile_id);const atLabel=refreshing?'获取中':a.at_refresh_status==='failed'?'失败':!a.at_saved?'未保存':a.at_token_changed===true?'已更新':'已保存';const windowState=deleted?'deleted':closed?'closed':openFailed?'open_failed':notOpened?'not_opened':'success';const windowLabel=deleted?'已删除':closed?'已关闭':openFailed?'打开失败':notOpened?'纯协议':'已打开';const closeLabel=deleted?'已删':closed?'删环境':'关闭';return `<tr><td><input type="checkbox" data-success-account-id="${a.id}" ${selectedSuccessAccounts.has(Number(a.id))?'checked':''}></td><td><div class="email-stack"><div><span>原</span><strong>${esc(a.old_email||'-')}</strong></div><div><span>新</span><strong>${esc(a.new_email||a.current_email||'-')}</strong></div></div></td><td>${pill(refreshing?'running':a.at_refresh_status==='failed'?'failed':'success')}<small class="compact-label">${esc(atLabel)}</small>${a.at_refresh_error?`<small class="action-error compact-error" title="${esc(a.at_refresh_error)}">⚠</small>`:''}</td><td title="${esc(a.roxy_open_error||'')}">${pill(windowState)}<small class="compact-label">${esc(windowLabel)}</small>${openFailed?'<small class="action-error compact-error">⚠</small>':''}</td><td><span class="cdp-port">${a.roxy_cdp_port?`<code>${esc(a.roxy_cdp_port)}</code>`:'-'}</span></td><td>${esc(shortTime(a.at_saved_at||a.at_refreshed_at||a.rebound_at))}</td><td><div class="row-actions"><button class="btn ghost" title="复制完整结果" data-copy-export="${a.id}">结果</button><button class="btn primary" title="复制 AT" data-copy-at="${a.id}">AT</button><button class="btn violet" title="推送 AT 到 GCash 提链" data-push-gcash="${a.id}">GCash</button><button class="btn primary" title="检查并更新 AT（Roxy 窗口或纯协议登录）" data-refresh-at="${a.id}" ${refreshing?'disabled':''}>${refreshing?'获取中':'刷新'}</button><button class="btn ghost" title="关闭窗口或删除环境" data-close-roxy="${a.id}" ${refreshing||!hasProfile||deleted?'disabled':''}>${closeLabel}</button><button class="btn danger" title="清理账号" data-delete-success-account="${a.id}" data-label="${esc(a.new_email||a.current_email||a.old_email)}" ${refreshing||hasProfile?'disabled title="请先关闭窗口并删除 Roxy 环境"':''}>清理</button></div></td></tr>`}).join('')||'<tr><td colspan="7">暂无换绑完成账号。</td></tr>';$('#successAccountsBody').querySelectorAll('[data-success-account-id]').forEach(el=>el.onchange=()=>{const id=Number(el.dataset.successAccountId);el.checked?selectedSuccessAccounts.add(id):selectedSuccessAccounts.delete(id);renderSuccessAccounts()});const all=rows.length>0&&rows.every(a=>selectedSuccessAccounts.has(Number(a.id)));['#selectSuccessAll','#selectSuccessAllTable'].forEach(selector=>{const input=$(selector);if(input)input.checked=all});$('#copySelectedAT').disabled=!selectedSuccessAccounts.size;$('#pushSelectedGCash').disabled=!selectedSuccessAccounts.size;$('#cleanupSelectedAccounts').disabled=!selectedSuccessAccounts.size}
 function taskActions(t){const account=STATE.accounts.find(a=>Number(a.id)===Number(t.account_id));const buttons=[];if(['queued','running'].includes(t.status))buttons.push(`<button class="btn danger" data-stop-task="${t.id}">停止</button>`);if(t.stage==='manual_review'&&account?.status==='review')buttons.push(`<button class="btn primary" data-review-login-account="${t.account_id}">重新登录获取 AT</button>`);if(t.status==='failed'&&account?.status==='failed')buttons.push(`<button class="btn primary" data-retry-account="${t.account_id}">失败重试</button>`);if(['success','failed','review','stopped'].includes(t.status))buttons.push(`<button class="btn danger" data-delete-finished-task="${t.id}">清理记录</button>`);return buttons.length?`<div class="row-actions">${buttons.join('')}</div>`:'-'}
 function taskRows(target){target.innerHTML=STATE.tasks.map(t=>`<tr><td>#${t.id}</td><td><strong>${esc(t.old_email)}</strong> → ${esc(t.new_email)}</td><td>${esc(t.proxy_display||'-')}<small>${t.proxy_attempt?` · 第 ${esc(t.proxy_attempt)} 条`:''}</small></td><td>${pill(t.status)}</td><td>第 ${esc(t.attempt||1)} 次 · ${esc(t.stage||'-')}</td><td title="${esc(t.message||'')}">${esc((t.message||'-').slice(0,90))}</td><td>${esc(t.completed_at||'-')}</td><td>${taskActions(t)}</td></tr>`).join('')||'<tr><td colspan="8">暂无任务</td></tr>'}
 function resultVisibleLimit(){const input=$('#resultVisibleRows');const value=Number(input?.value);const rows=Number.isFinite(value)&&value>=1?Math.min(100,Math.floor(value)):10;if(input)input.value=rows;return rows}
 function renderRecent(){const rows=STATE.tasks.slice(0,resultVisibleLimit());const finished=new Set(['success','failed','review','stopped']);const selectable=rows.filter(t=>finished.has(t.status));const valid=new Set(selectable.map(t=>Number(t.id)));selectedResultTasks=new Set([...selectedResultTasks].filter(id=>valid.has(Number(id))));$('#recentTasks').innerHTML=rows.map(t=>{const canSelect=finished.has(t.status);return `<div class="task-row"><div>${canSelect?`<input type="checkbox" data-result-task-id="${t.id}" ${selectedResultTasks.has(Number(t.id))?'checked':''}>`:'-'}</div><strong>#${t.id}</strong><div>${esc(t.old_email)}<br><small>→ ${esc(t.new_email)} · ${esc(t.proxy_display||'待分配代理')}</small></div><div>${pill(t.status)}</div><div>${esc(t.message||t.stage||'-')}</div><div>${['queued','running'].includes(t.status)?`<button class="btn danger" data-stop-task="${t.id}">停止</button>`:canSelect?`<button class="btn danger" data-delete-finished-task="${t.id}">清理记录</button>`:'-'}</div></div>`}).join('')||'<div class="task-row"><small>暂无换绑任务</small></div>';$('#recentTasks').querySelectorAll('[data-result-task-id]').forEach(el=>el.onchange=()=>{const id=Number(el.dataset.resultTaskId);el.checked?selectedResultTasks.add(id):selectedResultTasks.delete(id);renderRecent()});const all=selectable.length>0&&selectable.every(t=>selectedResultTasks.has(Number(t.id)));const selectAll=$('#selectResultAll');if(selectAll)selectAll.checked=all;$('#cleanupSelectedResults').disabled=!selectedResultTasks.size}
-function renderSuccessTrialInfo(){const rows=STATE.accounts.filter(a=>a.status==='success');const body=$('#successAccountsBody');if(!body)return;if(!rows.length){const empty=body.querySelector('td[colspan]');if(empty)empty.colSpan=8;return}body.querySelectorAll('tr').forEach((row,index)=>{if(row.children.length>=8)return;const account=rows[index];if(!account)return;const status=account.trial_check_status||'not_configured';const pillState=status==='running'?'running':status==='success'?'success':status==='failed'?'failed':'not_opened';const label=status==='running'?'检测中':status==='success'?(account.trial_zero_trial_eligible?'0元试用':'无0元试用'):status==='failed'?'检测失败':'未配置检测代理';const country=account.trial_check_proxy_country?` · ${esc(account.trial_check_proxy_country)}`:'';const cell=document.createElement('td');cell.title=account.trial_check_error||'';cell.innerHTML=`${pill(pillState)}<small class="compact-label">${label}${country}</small>`;row.insertBefore(cell,row.children[4]||null)})}
+function renderSuccessTrialInfo(){const rows=successAccountRows();const body=$('#successAccountsBody');if(!body)return;if(!rows.length){const empty=body.querySelector('td[colspan]');if(empty)empty.colSpan=8;return}body.querySelectorAll('tr').forEach((row,index)=>{if(row.children.length>=8)return;const account=rows[index];if(!account)return;const status=account.trial_check_status||'not_configured';const pillState=status==='running'?'running':status==='success'?'success':status==='failed'?'failed':'not_opened';const label=status==='running'?'检测中':status==='success'?(account.trial_zero_trial_eligible?'0元试用':'无0元试用'):status==='failed'?'检测失败':'未配置检测代理';const country=account.trial_check_proxy_country?` · ${esc(account.trial_check_proxy_country)}`:'';const cell=document.createElement('td');cell.title=account.trial_check_error||'';cell.innerHTML=`${pill(pillState)}<small class="compact-label">${label}${country}</small>`;row.insertBefore(cell,row.children[4]||null)})}
 function applyPairVisibleLimit(){const input=$('#pairVisibleRows');const wraps=document.querySelectorAll('.pairing-table,.success-table-wrap');if(!input||!wraps.length)return;const value=Number(input.value);if(!Number.isFinite(value)||value<1)return;const rows=Math.max(1,Math.min(100,Math.floor(value)));input.value=rows;wraps.forEach(wrap=>wrap.style.maxHeight=`${rows*48+44}px`);try{localStorage.setItem('email-rebind:pair-visible-rows',String(rows))}catch(_){} }
 function render(){renderStats();renderPairs();renderPool();renderProxyPool();renderDetectionProxies();renderSuccessAccounts();renderSuccessTrialInfo();applyPairVisibleLimit();taskRows($('#taskBody'));renderRecent();$('#clearFinishedTasks').disabled=!STATE.tasks.some(t=>['success','failed','review','stopped'].includes(t.status));$('#deleteAllProxies').disabled=!STATE.proxies.length}
 async function load(){try{STATE=await api('/api/state');const retryInput=$('#transientRetries');if(retryInput&&!retrySettingInitialized&&STATE.settings){retryInput.value=Number(STATE.settings.max_transient_retries??2);retrySettingInitialized=true}const valid=new Set(STATE.accounts.filter(a=>['ready','failed'].includes(a.status)).map(a=>Number(a.id)));if(!selectionInitialized){selected=new Set(valid);selectionInitialized=true}else{selected=new Set([...selected].filter(id=>valid.has(id)))}await preview(false);render()}catch(e){toast('加载失败：'+e.message)}}
@@ -60,7 +62,8 @@ async function deleteSelectedReplacements(){const ids=[...selectedReplacements];
 async function deleteFinishedTask(button){if(!confirm(`确定清理已结束任务 #${button.dataset.deleteFinishedTask} 的记录？账号和导出结果会保留。`))return;button.disabled=true;try{await api(`/api/tasks/${button.dataset.deleteFinishedTask}`,{method:'DELETE'});toast('任务记录已清理');await load()}catch(error){toast(error.message);button.disabled=false}}
 async function waitForAccessTokenRefresh(accountId){for(let attempt=0;attempt<180;attempt+=1){await sleep(1000);await load();const account=STATE.accounts.find(item=>Number(item.id)===Number(accountId));if(!account)return toast('账号记录不存在');if(account.at_refresh_status==='running')continue;if(account.at_refresh_status==='failed')return toast(`重新获取 AT 失败：${account.at_refresh_error||'未知错误'}`);if(!account.at_saved)return toast('重新获取完成，但 AT 保存校验未通过');return toast(account.at_token_changed===true?'重新获取完成：AT 已更新并保存，可直接复制':'重新获取完成：AT 未变化，现有 AT 已保存，可直接复制')}toast('重新获取 AT 仍在进行，界面会继续自动刷新')}
 $('#successAccountsBody').onclick=async e=>{const push=e.target.closest('[data-push-gcash]');if(push){push.disabled=true;try{await pushAccessTokenToGCash(push.dataset.pushGcash)}catch(error){toast(error.message)}finally{push.disabled=false}return}const copy=e.target.closest('[data-copy-export]');if(copy){copy.disabled=true;try{await copyExport(`/api/accounts/${copy.dataset.copyExport}/export`);toast('已复制该账号结果')}catch(error){toast(error.message)}finally{copy.disabled=false}return}const copyAt=e.target.closest('[data-copy-at]');if(copyAt){copyAt.disabled=true;try{await copyAccessToken(copyAt.dataset.copyAt);toast('已复制该账号的单独 AT')}catch(error){toast(error.message)}finally{copyAt.disabled=false}return}const refresh=e.target.closest('[data-refresh-at]');if(refresh){refresh.disabled=true;try{const accountId=Number(refresh.dataset.refreshAt);await api(`/api/accounts/${accountId}/refresh-at`,{method:'POST'});toast('已开始检查 Roxy；无窗口时将用纯协议登录更新 AT');await load();await waitForAccessTokenRefresh(accountId)}catch(error){toast(error.message)}return}const cleanup=e.target.closest('[data-delete-success-account]');if(cleanup){await deleteAccount(cleanup,true);return}const close=e.target.closest('[data-close-roxy]');if(!close)return;if(!confirm('关闭这个成功账号的 Roxy 窗口并永久删除对应环境？删除后不能再重新获取 AT。'))return;close.disabled=true;try{await api(`/api/accounts/${close.dataset.closeRoxy}/close-roxy`,{method:'POST'});toast('Roxy 窗口已关闭，环境已删除');await load()}catch(error){toast(error.message)}};
-function setSuccessSelection(checked){selectedSuccessAccounts=checked?new Set(STATE.accounts.filter(a=>a.status==='success').map(a=>Number(a.id))):new Set();renderSuccessAccounts();renderSuccessTrialInfo()}
+function setSuccessSelection(checked){selectedSuccessAccounts=checked?new Set(successAccountRows().map(a=>Number(a.id))):new Set();renderSuccessAccounts();renderSuccessTrialInfo()}
+$('#successTrialFilter').onchange=e=>{successTrialFilter=e.target.value==='trial'?'trial':'all';selectedSuccessAccounts=new Set();renderSuccessAccounts();renderSuccessTrialInfo()};
 $('#selectSuccessAll').onchange=e=>setSuccessSelection(e.target.checked);
 $('#selectSuccessAllTable').onchange=e=>setSuccessSelection(e.target.checked);
 $('#copySelectedAT').onclick=async()=>{const button=$('#copySelectedAT');button.disabled=true;try{await copySelectedAccessTokens()}finally{button.disabled=!selectedSuccessAccounts.size}};
diff --git a/templates/index.html b/templates/index.html
index 4e2cc87..d7eacce 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -57,7 +57,7 @@
       </article>
 
       <article class="card success-account-card">
-        <div class="card-head responsive"><div><h2>换绑完成账号</h2><p>纯协议结果直接保存 AT；“检查并更新 AT”会先检查 Roxy：有窗口就复用窗口更新，没有窗口就用纯协议登录一次更新。</p></div><div class="actions"><label><input type="checkbox" id="selectSuccessAll"> 全选</label><button class="btn primary" id="copySelectedAT" disabled>复制AT</button><button class="btn violet" id="pushSelectedGCash" disabled>推送GCash</button><button class="btn danger" id="cleanupSelectedAccounts" disabled>清理</button></div></div>
+        <div class="card-head responsive"><div><h2>换绑完成账号</h2><p>纯协议结果直接保存 AT；“检查并更新 AT”会先检查 Roxy：有窗口就复用窗口更新，没有窗口就用纯协议登录一次更新。</p></div><div class="actions"><label>筛选 <select id="successTrialFilter" aria-label="筛选成功账号"><option value="all">全部账号</option><option value="trial">仅有试用资格</option></select></label><label><input type="checkbox" id="selectSuccessAll"> 全选</label><button class="btn primary" id="copySelectedAT" disabled>复制AT</button><button class="btn violet" id="pushSelectedGCash" disabled>推送GCash</button><button class="btn danger" id="cleanupSelectedAccounts" disabled>清理</button></div></div>
         <div class="table-wrap success-table-wrap"><table class="success-table"><thead><tr><th><input type="checkbox" id="selectSuccessAllTable"></th><th>邮箱</th><th>AT</th><th>窗口</th><th>资格</th><th>CDP端口</th><th>更新</th><th>操作</th></tr></thead><tbody id="successAccountsBody"></tbody></table></div>
         <div class="result-footer"><button class="btn ghost" id="copySuccessResultsBottom">一键复制成功账号结果</button></div>
       </article>
